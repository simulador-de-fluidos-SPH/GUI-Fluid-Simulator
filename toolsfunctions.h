#ifndef TOOLSFUNCTIONS_H
#define TOOLSFUNCTIONS_H

#include <QMouseEvent>
#include <globalVariables.h>

void* selectParticle(QMouseEvent *e);
void deleteParticle(QMouseEvent *e);
void particlePointerSetter(QMouseEvent *e);

#endif // TOOLSFUNCTIONS_H
